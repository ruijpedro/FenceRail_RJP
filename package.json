{
  "name": "vedacoes",
  "version": "1.0.0",
  "private": true,
  "description": "Vedações IP - Cadastro e inspeção de vedações ferroviárias",
  "type": "module",

  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host",
    "android": "npm run build && npx cap sync android",
    "sync": "npx cap sync",
    "open": "npx cap open android"
  },

  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/geolocation": "^7.1.0",

    "jspdf": "^2.5.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "xlsx": "^0.18.5"
  },

  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  },

  "engines": {
    "node": ">=20"
  }
}
